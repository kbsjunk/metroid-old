<svg width="{{ width }}" height="{{ width }}" xmlns="http://www.w3.org/2000/svg">
	<circle
	cx="{{ width/2 }}"
	cy="{{ width/2 }}"
	r="{{ (width-(width/10))/2 }}"
	style="fill:white;stroke:black;stroke-width:{{ (width/20)|number_format }};">
</circle>
</svg>